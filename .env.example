# NextDoorJobs Environment Variables
# Copy this file to .env.local and fill in your values

# Clerk Authentication (Optional - app works without it, but auth features will be disabled)
# Get your keys from https://dashboard.clerk.com/
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
# If you use a custom Clerk Frontend API domain that doesn't serve the script, set this to load clerk-js from a CDN:
# NEXT_PUBLIC_CLERK_JS_URL=https://unpkg.com/@clerk/clerk-js@5/dist/clerk.browser.js
# So FAPI requests (sign-in, sessions) work without the custom domain, set your app URL + /__clerk (local: http://localhost:3000/__clerk):
# NEXT_PUBLIC_CLERK_PROXY_URL=http://localhost:3000/__clerk
# App URL - used for email "View message" links and Clerk proxy. Local dev uses localhost; production MUST use your real domain.
# Local (default in code if unset): http://localhost:3000
# Production: set NEXT_PUBLIC_APP_URL in your hosting dashboard (e.g. https://mapmygig.com) - see README or deployment docs.
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# Database Connection (Required for production)
# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@host:5432/database

# Redis Cache (optional for development, required for production)
# Redis connection string for caching
REDIS_URL=redis://localhost:6379

# ImgBB - Image upload for avatar (optional, get free key at https://api.imgbb.com/)
# Required for "Upload photo from device" in Settings
IMGBB_API_KEY=your_imgbb_api_key

# Razorpay (optional - for subscription payments in Settings > Subscription)
# Get keys from https://dashboard.razorpay.com/ (use test keys for development)
NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_test_...
RAZORPAY_KEY_SECRET=your_razorpay_key_secret

# Email (optional - for recruiter/candidate message notifications)
# Use a transactional provider (SendGrid, Resend, Gmail SMTP). For Gmail: enable 2FA, create App Password.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_email@example.com
SMTP_PASS=your_app_password
SMTP_FROM=MapMyGig <noreply@yourdomain.com>

# Admin area (optional - hidden at /admin, not shown in sign-up)
# Run: npm run db:seed-admin-owner then set ADMIN_OWNER_USER_ID to the printed id (or use ADMIN_OWNER_EMAIL).
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_secure_password
ADMIN_SESSION_SECRET=your_random_32_char_secret_here
ADMIN_OWNER_USER_ID=
# Or use email lookup instead of id (seed creates user with this email if set):
# ADMIN_OWNER_EMAIL=admin-owner@internal

# Note: .env.local is gitignored and should never be committed
# For production deployments (Vercel), set these in your project's environment variables
